import WalletClient from '../../components/account/WalletClient';

export const metadata = {
  title: 'CityMap Wallet',
  description: 'Tus cupones, boletos y tarjetas de lealtad en CityMap.',
};

export default function WalletPage() {
  return <WalletClient />;
}
